Trained 20k for lift only -> works decent. avg. -19 ~ -20 reward per episode

Trained 20k for lift + arm -> Weirdly only goes down and getting very high negative reward. avg. above -88.5 reward per episode. took 52 min for 20k continuing training.

Trained 20k more for lift + arm -> now the reward went up to -19 ~ -20 per peisode. The arm keep extend but don't know when to go lower though
res: add penalty when the robot trys to move beyond the limit (keep extend)

Added a simple penalty system for arm
# only relevant if your action space includes arm extend
if self.allowed_parts in ("lift_arm", "all") and a == 4:
    arm_pos = float(self._get_status().arm.pos)
    arm_max = float(self.limits[Actuators.arm][1])
    if arm_pos > (arm_max - self.ARM_NEAR_MAX_THRESH):
        reward -= self.ARM_NEAR_MAX_PENALTY


Realized that the reset() is not resetting the apple. If the robot touches the apple and if it falls, then the training won't be as effective as. Also, we can't train the apple in a randomized env.

-> Working on how to reset the apple in every reset() training. We need to teleport the apple to a certain fixed position or a randomized range
-> current position => pos=[ 0.9   -0.3    0.963]  quat=[ 0.973  0.     0.    -0.229]
-> steps generated by GPT
Step A: Add a new proxy field for object pose commands
Step B: Apply the pose inside the MuJoCo server
Step C: Add a client-side helper on your sim wrapper
Step D: Use it in env.reset()

res: now we can use sim.set_object_pose(self.obj_name, pos_xyz=(0.9, -0.4, 0.92), quat_wxyz=(1,0,0,0))


The issue is when we do sim.set_object_pose(self.obj_name, pos_xyz=(0.9, -0.5, 0.92), quat_wxyz=(1,0,0,0)). The apple is bouncing off the table and fall under.
-> realized that the 0.92 is lower than the first generated position of the apple 0.96. We need to put z = 0.96 + 0.02 for extra room to be generated. This value will be depends on the radius or height of the object later.
-> Now it's correctly generated randomly

