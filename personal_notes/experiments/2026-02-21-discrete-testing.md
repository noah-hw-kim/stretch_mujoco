1. Trained total 20k for lift only -> works decent. avg. -19 ~ -20 reward per episode


2. Trained total 20k for lift + arm -> Weirdly only goes down and getting very high negative reward. avg. above -88.5 reward per episode. took 52 min for 20k continuing training.

3. Trained total 40k lift + arm -> now the reward went up to -19 ~ -20 per peisode. The arm keep extend but don't know when to go lower though
res: add penalty when the robot trys to move beyond the limit (keep extend)

1) Added a simple penalty system for arm
# only relevant if your action space includes arm extend
if self.allowed_parts in ("lift_arm", "all") and a == 4:
    arm_pos = float(self._get_status().arm.pos)
    arm_max = float(self.limits[Actuators.arm][1])
    if arm_pos > (arm_max - self.ARM_NEAR_MAX_THRESH):
        reward -= self.ARM_NEAR_MAX_PENALTY


2) Realized that the reset() is not resetting the apple. If the robot touches the apple and if it falls, then the training won't be as effective as. Also, we can't train the apple in a randomized env.

-> Working on how to reset the apple in every reset() training. We need to teleport the apple to a certain fixed position or a randomized range
-> current position => pos=[ 0.9   -0.3    0.963]  quat=[ 0.973  0.     0.    -0.229]
-> steps generated by GPT
Step A: Add a new proxy field for object pose commands
Step B: Apply the pose inside the MuJoCo server
Step C: Add a client-side helper on your sim wrapper
Step D: Use it in env.reset()

res: now we can use sim.set_object_pose(self.obj_name, pos_xyz=(0.9, -0.4, 0.92), quat_wxyz=(1,0,0,0))

3) follwoing issue (solved): when we do sim.set_object_pose(self.obj_name, pos_xyz=(0.9, -0.5, 0.92), quat_wxyz=(1,0,0,0)). The apple is bouncing off the table and fall under.
-> realized that the 0.92 is lower than the first generated position of the apple 0.963. We need to put z = 0.963 + 0.02 for extra room to be generated. This value will be depends on the radius or height of the object later.
-> Now it's correctly generated

4) follwoing issue: robot trys to move beyond the limit (keep extend)

Stage 0: Fixed, easy (no edge-of-reach)
Goal: confirm the penalty stops “slam to max” and policy still reaches.
Apple pose: fixed y closer than edge case (example: y = -0.35 to -0.40)
Train: 50k steps (minimum 30k)

Move on when:
success (or near-success) is stable
arm is not living at arm_max most of the episode


Stage 1: Y random small range
Goal: generalize a little without making exploration hard.
Randomize: y ∈ [-0.40, -0.30]
Train: 100k steps (minimum 50k)
Good transition trick (prevents forgetting):

first 20k of this stage: 80% fixed / 20% random
next 20k: 50/50
rest: mostly random

Stage 2: Y random medium range
Goal: handle more variation but still mostly reachable without max.
Randomize: y ∈ [-0.50, -0.30]
Train: 150k steps (minimum 100k)

Stage 3: Y random full reachable range (edge cases last)
Goal: include the harder “near edge” cases like y ≈ -0.6.
Randomize: y ∈ [-0.60, -0.30] (only if your physics spawn is stable there)
Train: 200k steps (minimum 150k)

If you notice y=-0.60 forces max extension and conflicts with your penalty:

keep Stage 3 as [-0.55, -0.30] first
then add [-0.60, -0.55] later as a “hard slice”







